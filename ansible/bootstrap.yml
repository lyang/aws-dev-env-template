---
- hosts: localhost
  gather_facts: no
  connection: local
  tasks:
  - include_tasks: tasks/local.yml

- hosts: pristine
  gather_facts: no
  become: yes
  tasks:
    - import_tasks: tasks/ebs.yml
    - import_tasks: tasks/admin_user.yml
